{
  "name": "pdfcrop",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "DavidAnson.vscode-markdownlint",
        "esbenp.prettier-vscode",
        "dbaeumer.vscode-eslint"
      ],
      "settings": {
        "[markdown]": {
          "editor.defaultFormatter": "esbenp.prettier-vscode"
        },
        "prettier.proseWrap": "always",
        "markdownlint.config": {
          "no-duplicate-heading": {
            "siblings_only": true
          }
        },
        "files.insertFinalNewline": true
      }
    }
  },
  "workspaceFolder": "/workspaces/pdfcrop",
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspaces/pdfcrop,type=bind,consistency=cached",
  "remoteUser": "ubuntu",
  "mounts": [
    "source=config-pdfcrop,target=/home/ubuntu/persistent-config,type=volume",
    "source=/etc/localtime,target=/usr/share/zoneinfo/hostlocaltime,type=bind,readonly"
  ],
  "postCreateCommand": "bash .devcontainer/setup.sh"
}
